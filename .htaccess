RewriteEngine On

# Si le fichier ou dossier existe, on le charge directement (CSS, JS, images)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon, on redirige tout vers index.php
RewriteRule ^(.*)$ index.php [QSA,L]